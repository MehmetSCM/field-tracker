import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.keywestasphalt.fieldtracker',
  appName: 'Field Tracker',
  webDir: 'dist'
};

export default config;
